<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Ticket</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f5f5f5;
            flex-direction: column;
        }

        .form-container {
            width: 100%;
            max-width: 600px;
            padding: 20px;
            margin: auto;
            background-color: white;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .header {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            padding: 10px;
        }

        .header img {
            height: 70px;
        }

        .form-label-group {
            margin-bottom: 1rem;
        }

        .form-label-group label {
            margin-bottom: .5rem;
        }

        .form-label-group input,
        .form-label-group select,
        .form-label-group textarea {
            height: calc(2.25rem + 2px);
            padding: .375rem .75rem;
        }

        .btn-primary {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="imgs/logo.png" alt="Cuidamor Logo">
    </div>
    <div class="form-container">
        <h1 class="h3 mb-3 font-weight-normal text-center">Crear Ticket</h1>
        <form action="/crear" method="POST">
            <div class="form-label-group">
                <label for="tipo">Tipo de Ticket</label>
                <select name="tipo" id="tipo" class="form-control" required>
                    <option value="">Seleccionar tipo</option>
                    <option value="Sugerencia">Sugerencia</option>
                    <option value="Reporte">Reporte</option>
                </select>
            </div>

            <div class="form-label-group">
                <label for="descripcion">Descripción</label>
                <textarea name="descripcion" id="descripcion" class="form-control" placeholder="Descripción del ticket" rows="4" required></textarea>
            </div>

            <button class="btn btn-lg btn-primary btn-block" type="submit">Crear Ticket</button>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
