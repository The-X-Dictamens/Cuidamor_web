<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Detalle de la Solicitud</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container py-4">
        <h1>Detalle de la Solicitud: <%= solicitud.id_sol %></h1>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Descripción: <%= solicitud.des_sol %></h5>
                <p class="card-text">Tipo: <%= solicitud.tipo_sol === 'Cuidador' ? 'Cuidador' : 'Enfermero' %></p>
                <p class="card-text">Estado: <%= solicitud.est_sol %></p>
                <p class="card-text">Costo: <%= solicitud.cost_sol %></p>
                <p class="card-text">Fecha de Inicio: <%= new Date(solicitud.fecini_hor).toISOString().slice(0, 10) %></p>
                <p class="card-text">Fecha de Fin: <%= new Date(solicitud.fecfin_hor).toISOString().slice(0, 10) %></p>
                <p class="card-text">Hora de Inicio: <%= solicitud.horini_dh %></p>
                <p class="card-text">Hora de Fin: <%= solicitud.horfin_dh %></p>
                <p class="card-text">Días: <%= solicitud.dia_dh %></p>
                
                <p class="card-text">Paciente: <%= solicitud.nom_pac %></p>
            </div>
        </div>
        <a href="/solicitudes" class="btn btn-link">Volver a la lista</a>
        <a href="/solicitudes/editar/<%= solicitud.id_sol %>" class="btn btn-warning">Editar</a>
    </div>
</body>
</html>
